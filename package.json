{
  "name": "mintoscope",
  "version": "0.1.0",
  "description": "Solana Token-2022 extension risk auditor — a Claude Code / Codex skill. Detects mint extensions, decomposes authorities, flags dangerous configurations and verified illegal combinations, and prescribes fixes.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "audit": "tsx examples/audit-deployed.ts",
    "fixtures": "tsx examples/fixtures/mint-fixtures.ts"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.95.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
